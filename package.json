{
  "name": "dynamodb-extract",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "python build.py",
    "deploy": "python build.py && sls deploy --config=serverless.beta.yml",
    "domain": "sls create_domain --config=serverless.beta.yml"
  },
  "author": "Aaron Sisler",
  "license": "ISC",
  "dependencies": {
    "serverless-domain-manager": "^5.1.0"
  },
  "devDependencies": {
    "serverless": "^2.41.2"
  }
}
